[project]
name = "openclaw-onepilot-skills"
version = "0.1.0"
description = "Read-only skill discovery surface for the Onepilot iOS app on OpenClaw hosts."
authors = [{ name = "Onepilot" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
